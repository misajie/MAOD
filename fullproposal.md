我建议把主线升级为：**基于原始 OSM、出行知识与预测残差，自动发现空间特征程序，并与 OD 模型联合适配的多智能体系统。**

Deep Gravity 可以作为主要预测器，允许重新训练、扩展输入和迁移参数。Surveyor 用 RAG 发现候选出行机制；Cartographer 把机制编译成可执行的空间特征；Adapter 根据 OD 误差重新组织检索、修改程序并更新模型。这样，创新就集中在**如何从城市证据和预测失败中发现有效表征**。

上一轮我把“冻结 backbone”当成了路线前提，这一点应当纠正。同样，所有 baseline 都使用同一个 backbone，也不应成为主实验的必要条件。

本次只进行了本地源码和资源阅读，两个子 agent 分别整理了 Deep Gravity 与 LLMrag；没有修改文件，没有运行训练或测试，也没有核查引文。下面是我建议采用的完整路线。

**源码首先改变了几个重要判断。** 这些事实决定了新 proposal 应怎样写，也决定了后续实现不能直接照搬什么。

| 从本地源码确认的事实 | 对新 proposal 的影响 |
|---|---|
| `dim_input` 根据实际特征向量长度计算，MLP 首层随之实例化 | DG 支持不同数量的输入特征；需要设计的是程序变化后的训练与参数迁移 |
| 训练、测试按 tile 划分，每个 origin 的候选 destination 限于自身 tile | 原代码输出的是 tile 内 OD 子矩阵；若目标是都市区完整矩阵，需要扩大 destination 集合 |
| `oa2pop` 来自 flow 按 origin 求和 | 它实际是真实出流量，不能直接称为独立人口数据 |
| 源码直接使用流量计数计算交叉熵 | 实现是流量加权 NLL，与 README 中按行归一化的表达不同 |
| 本地 New York 示例指向 GeoDS COVID-19 流量 | 不能直接写成 NYS census commuting 或 LODES 通勤数据 |
| 预处理入口被注释，部分预处理函数存在明显错误 | 仓库提供模型骨架和 processed 示例，但没有闭合的原始数据重建流水线 |
| 当前实现主要计算 CPC，绘图读取 DG/G/MFG/NG 的结果 | Pearson、NRMSE、JSD 及完整 baseline runner 都需要自行补齐 |

多特征接口见 [main.py](/F:/pros/MAOD/resources/deepgravity/deepgravity/main.py:237) 和 [data_loader.py](/F:/pros/MAOD/resources/deepgravity/deepgravity/data_loader.py:45)；出流量变量及预处理入口见 [utils.py](/F:/pros/MAOD/resources/deepgravity/deepgravity/utils.py:134)；数据来源见 [README.md](/F:/pros/MAOD/resources/deepgravity/README.md:105)。

还有一个直接影响最终排名的问题：[od_models.py](/F:/pros/MAOD/resources/deepgravity/deepgravity/models/od_models.py:18) 的 CPC 分母写成了两倍预测总量。在真实与预测总量相等时，它恰好等价于标准 CPC，但不能用于任意模型输出。与此同时，最终 tile CPC 的分母取 origin 在 OD 字典中的全部出流，预测却来自 tile 内目的地；如果字典保留跨 tile 流量，就会出现评价范围不一致。后续应该保留 DG 的模型思想，重建一个明确的矩阵输出与评价层。

---

**研究问题应从“城市需要不同标签”推进到“城市需要不同的空间表征构造方式”。**

旧稿对 Deep Gravity 的批评也应改写。固定 feature schema 并不等于不同城市拥有相同特征值，更不等于神经网络只能学习相同的吸引机制。更准确、更有力度的问题是：

> 预先聚合的地理特征会丢失用途、尺度和空间关系上的区别，而这些区别无法仅靠下游模型重新加权恢复。

例如：

- 将医院、诊所、药房合并，可能损失大型就业节点与社区服务设施之间的区别。
- 将大学只表示为 POI 数量，会损失校园占地、建筑规模以及交通连接关系。
- 区域内道路总长度不能充分表示跨河通达性、道路绕行和通勤走廊。
- 一个低频但大型的物流园，与多个小型仓储建筑，在简单计数中可能被错误排序。
- 同一个 OSM tag 在不同城市的标注方式不同，适合的代理变量也不同。

因此，研究对象应包含**语义分组、空间尺度、聚合规则、可达性、组合特征和缺失时的代理构造**。

建议标题改为：

> **MapAgents: Retrieval-Grounded Spatial Program Discovery for Origin–Destination Prediction**

如果希望直接突出闭环：

> **MapAgents: Learning Urban OD Flows through Multi-Agent Feature Discovery and Model Adaptation**

对应的核心问题可以写成：

> Can agents use geographic evidence, mobility knowledge, and prediction errors to discover spatial feature programs that improve OD prediction and adapt across cities?

这个问题比“有 gate 的三个 agent 是否优于固定 OSM 表”更有研究空间，而且可以通过实际实验回答。

---

**方法仍然保留三个 agent，但要赋予它们不同的信息和决策职责。**

我不建议为了显得复杂再增加 Critic、Planner、Verifier 等角色。三层已经足够，关键在于形成真正的反馈回路。

| Agent | 主要掌握的信息 | 核心决策 | 输出 |
|---|---|---|---|
| Surveyor | 出行知识、OSM 语义、城市原始地理证据 | 当前城市可能缺少什么表征，下一步应检索什么 | 带证据的候选机制与特征提案 |
| Cartographer | OSM 对象、几何、空间算子、执行结果 | 如何把候选机制变成可计算变量 | 特征程序、数值表、覆盖与执行摘要 |
| Adapter | 训练/验证 OD、预测残差、历史程序收益 | 修改哪些特征，怎样更新模型，保留哪个候选 | 程序修改、训练策略、下一轮检索需求 |

交互应当是：

\[
\text{城市证据与知识检索}
\rightarrow
\text{空间程序}
\rightarrow
\text{OD 模型}
\rightarrow
\text{误差诊断}
\rightarrow
\text{新的检索与程序修改}.
\]

例如，Adapter 发现模型系统性低估某类外围目的地，先把这一误差模式交给 Surveyor。Surveyor 查询这些区域的 OSM 原始对象与相关知识，提出“工业面积可能比商业 POI 计数更有用”的候选解释；Cartographer 构造对应变量，Adapter 再通过训练与验证决定是否保留。

这里的多 agent 贡献是**语义检索、空间执行和数值反馈之间的分工与协同**。能否优于单 agent，需要通过对照实验判断，不能靠角色命名直接成立。

---

**Surveyor 的 RAG 应同时检索“出行知识”和“这座城市实际有什么”。**

现有 [LLMrag catalog](/F:/pros/MAOD/resources/LLMrag/catalog.md) 已经提供了适合的起点：约 1,503 条 OSM Map Features 记录，以及 NHTS、LODES、CTPP、PUMS、WorldPop 的目录和手册。但这些主要是知识材料，尚不能替代城市原始 OSM 数据。

我建议建立三个可检索的证据集合。

- **地理语义知识。** OSM tag/value 的含义、适用几何、相关标签、单位与可计算属性。
- **出行测量与机制知识。** 通勤、就业、目的地选择、方式、距离、时间等变量的定义，以及它们怎样对应可观察的地理属性。
- **城市观测证据。** 从原始 OSM 提取的实际 tag 分布、对象样本、建筑与道路结构、空间集中度、数值字段可用性和候选特征统计。

检索最好采用关键词与向量检索结合的方式。OSM 精确标签适合关键词匹配，“大型就业节点”“公共交通接驳”等概念适合语义检索。随后利用城市观测过滤和排序候选，避免检索结果只在文档中合理、在目标城市却没有可计算的数据。

这里需要注意两点。

第一，**1,503 条目录记录不能成为新的硬白名单。** OSM 是开放标签系统，目录只是检索起点。原始 OSM 中出现的其他标签也可以进入候选，随后依据对象上下文、已有文档和执行结果判断其用途。部分目录行还是通配模板或缺少完整说明，也不能把每一行机械地当成一个可直接执行的标签。

第二，手册中的变量定义不是因果证据。NHTS 能帮助系统理解出行目的与方式，但不能单凭 codebook 推断“这个城市居民一定偏好某种方式”。对于通勤任务，医院、大学、商业设施首先应被理解为可能的就业或交通节点，不能直接把患者、学生和购物者流量混入通勤目标。

Surveyor 的输出应是开放的候选机制，而不是七类 attractor。每个候选至少包含：

- 要刻画的城市现象；
- 支持它的检索片段与 OSM 对象摘要；
- 可执行的空间构造建议；
- 适用 origin、destination 还是 OD pair；
- 数据覆盖上的疑点；
- 哪类预测误差可能因此改善。

“机场周边就业集聚”“跨河通勤瓶颈”“分散工业走廊”都可以由证据产生，不需要先加入某个枚举列表。

另外，**tag 稀少不能直接等同于 OSM 漏标**。它也可能反映真实稀少。Surveyor 应把这记录为“观测稀疏及其可能原因”，而不是自动宣布 coverage hole。

---

**Cartographer 应构造空间程序，而不只是返回若干列名。**

旧稿的

\[
P_s=\{(k,v,\mathrm{agg},\mathrm{scale})\}
\]

适合简单查询清单，但不足以承载更强的创新。我建议把程序表示为一个空间操作图：底层读取 OSM 对象，上层进行筛选、空间关联、聚合、归一化和组合。

输出可以区分三个通道：

\[
P_s(\mathrm{OSM},\mathrm{Population},\mathrm{Geometry})
\longrightarrow
\left(X_s^O,X_s^D,Z_s^{OD}\right).
\]

其中：

- \(X^O\)：出发区域的属性；
- \(X^D\)：到达区域的属性；
- \(Z^{OD}\)：区域对之间的空间关系。

Deep Gravity 的输入由此扩展为：

\[
s_{ij}
=
g_{\theta_s}
\left(
x_i^O,\,
x_j^D,\,
d_{ij},\,
z_{ij}^{OD}
\right).
\]

这仍然保留 DG 的打分与目的地 softmax 结构，只是输入表征更加丰富。原始 DG 的对称区域特征拼接，是这个形式的一个特例。

建议支持的构造包括：

| 构造类型 | 示例 | 对 OD 的潜在作用 |
|---|---|---|
| 用途细分 | 将工业、办公、仓储建筑分别聚合 | 区分就业空间组织 |
| 规模与容量代理 | 建筑面积、可用楼层字段、设施容量字段 | 区分大型节点与零散设施 |
| 多尺度聚合 | 区域内部、周边距离带、最近设施距离 | 处理行政边界与实际活动范围不一致 |
| 组成与混合度 | 用地比例、用途多样性、住宅与非住宅比值 | 刻画区域功能结构 |
| 空间关系 | 工业建筑与干道或铁路节点的邻近关系 | 刻画通勤走廊及交通连接 |
| OD 成本 | 网络距离、绕行系数、跨障碍关系 | 补充直线距离 |
| 条件组合 | 不同目的地用途与距离的交互 | 表达不同活动节点的距离敏感性 |

这些是候选构造空间，不代表每个城市都要全部使用。具体程序由检索与误差反馈决定。

举一个完整的候选程序思路：模型低估外围工业区域后，系统检索工业与仓储标签，构造“与主要道路邻近的工业/仓储建筑面积”，分别考察区域内部和周边范围，再结合 origin 到该目的地的网络绕行程度。整个过程可以输出明确的对象集合、几何操作和数值特征，最后由 OD 验证结果决定是否保留。

这比“再加一个 `building=warehouse` 计数”更有实质内容。

Gate 可以保留为执行器中的类型检查，但应从标题和贡献中心移开。它主要处理几何、单位、空间关联、未观测标签的误用和不可执行操作。原来的几个限制也应调整：

- 人口可以合法地作为 origin 或 destination 属性；禁止的是无意重复和混淆来源，不是人口出现在 destination 端。
- 输入可以有多种空间尺度，只要显式聚合到目标输出区域。
- buffer 不必只有四个固定数值，可以根据区域尺度和候选机制选择。
- 点对象与建筑面可能描述同一设施，应提供明确的去重或分别计数规则。

这些是空间计算的定义，不需要包装成主要算法创新。

---

**Adapter 应设计成“残差驱动的程序与模型联合适配”。这是我最建议加强的核心。**

形式上，可以写成外层搜索程序、内层训练预测器：

\[
\theta^*(P)
=
\arg\min_{\theta}
\mathcal{L}_{\mathrm{train}}(\theta;P),
\]

\[
P^*
=
\arg\max_P
\mathrm{CPC}_{\mathrm{val}}
\left(
T,\widehat T_{\theta^*(P),P}
\right),
\]

并给定程序搜索和训练预算。计算成本可以作为单独的效率结果，暂时不必再引入难解释的复合奖励系数。

Adapter 不应只接收一个 CPC 数字。它需要结构化的误差信息，例如：

- 哪些目的地持续被高估、低估；
- 短、中、长距离流量分别有什么偏差；
- 误差是否集中在某些空间结构或 OSM 观测稀疏区域；
- 新特征覆盖了哪些区域；
- 新特征与已有变量是否高度冗余；
- 上一轮修改改善了哪些区域，又损害了哪些区域。

一个简单的目的地残差摘要是：

\[
r_j
=
\sum_{i\in\mathcal V}
\left(T_{ij}-\widehat T_{ij}\right),
\]

其中 \(\mathcal V\) 只能来自可用于开发的验证数据。还可以同时给出区域内归一化误差，避免诊断总被大流量区域主导。

每轮 Adapter 决定一组明确操作：细分已有类别、改变聚合范围、引入空间关系、替换观测不足的代理、删除冗余构造，或者判断当前问题更适合继续更新模型参数。候选都从同一个父程序和 checkpoint 出发，再通过验证结果选择，避免把额外训练次数误认为特征收益。

**DG 的参数迁移可以直接从源码结构扩展，不需要把复杂新编码器作为前提。**

建议给每个输入列一个稳定的语义 ID，包含：

\[
(\text{作用端},\text{表达式},\text{单位},\text{尺度},\text{归一化规则}).
\]

程序扩维时：

- 共同特征按语义 ID 复制首层权重；
- 新特征对应的首层权重初始化为零；
- 后续层复用；
- 所有参数允许继续训练。

在旧特征及其变换不变、仅新增列的情况下，新列置零可以保持初始预测函数不变。这样，新特征需要通过训练取得作用，不会因为随机扩维立即破坏已有模型。

删除旧特征或改变其归一化时不能宣称同样的保持性质。实现上可以先增加替代特征、联合训练，再决定是否删除旧列。

我不建议四周内再把 Set Transformer、超网络或文本生成神经权重放进主方法。它们是后续可以研究的方向，但目前最有价值的创新已经是**残差如何驱动地理知识检索和空间程序修改**。动态首层与参数复用负责让这条路线可执行。

与此同时，旧稿“迁移的是程序，不是权重”应删掉。新的迁移对象可以明确写成：

\[
\mathcal M_s
=
(P_s,\theta_s,\text{特征变换},\text{证据与修改记录}).
\]

程序和参数都可以迁移，然后分别比较各自贡献。

---

**少样本适配应优先使用完整 origin 行，并把验证标签算进预算。**

源码的自然训练单位就是一个 origin 对一组 destination 的分布。四周内最清楚的方案是：

> 观察目标城市少量 origin 的完整目的地流量行，适配模型与程序，再预测其他 origin 的完整流量行。

保留 \(1\%\)、\(10\%\) 可以，但应明确它们表示可见 origin 的比例，同时报告实际 origin 数、非零边数和可见矩阵单元数。

可见行中再划分用于参数训练和程序选择的部分。**两部分都属于目标标签预算。** 不能向 Adapter 提供 1% 的训练边，却额外使用大量目标验证 OD 来挑程序，最后仍称为“1% 监督”。

若继续用任意稀疏边作为预算，就需要区分“未观察”与“观察到零”，并设计对应的 masked likelihood 或观测模型。这是另一项方法设计，暂时不值得与主线同时展开。

跨城市实验建议明确包含以下几种模式：

| 模式 | 程序 | 模型参数 | 目标 OD 标签 |
|---|---|---|---|
| 源模型直接迁移 | 源程序 | 源参数 | 无 |
| 仅参数适配 | 源程序 | 目标少样本更新 | 有 |
| 目标程序重新拟合 | 目标生成程序 | 重新训练 | 有 |
| 联合适配 | 程序修改 | 参数迁移并更新 | 有 |
| 充分监督参考 | 目标程序 | 更多目标数据训练 | 更多标签，单独标明 |

零样本还有一个容易忽略的问题：**新特征没有学过的权重，不能因为 LLM 认为它有用就自动改善预测。** 如果目标城市发现了源模型未见过的构造，可以把该构造回到源城市执行，用源标签训练对应权重，再到目标城市预测；这仍可是不使用目标 OD 标签的迁移，但必须计算这部分训练成本。

四周版本可以先把“源程序与源模型直接迁移”作为清晰的零样本基线，把新构造的主要收益放在少样本联合适配中验证。

---

**数据主线建议转向一个可完整重建的通勤数据源、多座城市，而不是先承诺三个国家。**

现有资源中没有完整的 England、Italy 原始流量与重建程序；New York 示例也不能直接承担通勤证据。继续把“三个 Deep Gravity 地理区域完整复现”作为第一阶段，会让四周主要消耗在数据追溯上。

我更推荐：

> 以 LODES 的多个都市区建立主实验，完成跨城市适配；England 或 Italy 在数据口径和获取条件明确后，作为跨国家外部验证。

可以以 **6 座城市为第一版目标，8 座为扩展目标**，覆盖不同规模、空间结构和 OSM 观测条件。城市选择应依据这些预先可观察的属性，不依据 MapAgents 跑出来是否获胜。单一来源的多城市设计也更适合研究城市条件下的程序差异。

[LODES catalog](/F:/pros/MAOD/resources/LLMrag/catalogs/lodes.md) 已经给出了关键字段、工作类型和 crosswalk。建议优先选定一种明确口径，例如 `JT01` 主职岗位，使用：

\[
T_{ij}
=
\sum_{\substack{h\in i\\w\in j}}
\mathrm{S000}(h,w).
\]

这代表主职岗位的居住—工作联系，不应直接称为每天的实际出行次数。`JT00` 也可以使用，但不要混用两种 job type。

原始数据到完整矩阵的重建，应当包含下面这条闭合流程。

1. **确定流量语义和年份。** LODES、census commuting、GeoDS movement 分开处理；若使用 Italy 通勤表，也要区分工作与学习流量。
2. **确定都市区边界和空间单元。** LODES 可以从 block 按 crosswalk 汇总到 tract，保留字符串 ID 与对应几何。
3. **汇集完整相关 OD 文件。** 跨州都市区需要处理涉及工作州的 `main` 和 `aux`，避免只读取单州文件而漏掉内部跨州联系。
4. **聚合重复 OD 记录。** 固定 origin/destination 索引、保留域内自环，按数据发布语义处理零与缺失。
5. **保存域内矩阵及域外信息。** 都市区内矩阵的行总量必须是对该 destination 集合求和；域外流量不能悄悄进入某个指标的分母。
6. **单独构建人口与地理属性。** 独立人口 \(P_i\) 和真实出流量 \(O_i\) 分开保存，不能沿用 `oa2pop` 的混淆命名。
7. **读取具有明确日期的原始 OSM。** 保留对象类型、ID、tags、geometry，并在统一缓存上执行所有程序。
8. **生成带语义 schema 的特征表。** 面积、长度使用合适的米制投影；球面距离明确经纬度顺序与单位；数值变换与模型一起保存。
9. **输出完整评估区域的预测矩阵。** 各方法的结果按同一索引进入独立评价器。

已有预处理里错误写入 `od2flow.pkl`、路径拼接、经纬度面积计算等问题，说明这部分应新建一个清楚的数据构建层，而不是直接解除注释使用旧函数。也不宜承诺与原论文数字逐位一致：原始 OSM 到 18 列的完整聚合映射在当前 checkout 中并不充分。

对于 DG loader，最直接的都市区实现是把一座都市区作为一个 destination region，tract 作为内部区域。训练可以继续采样目的地，推断则枚举整个都市区的 destination。即使分块计算 logits，softmax 的分母也必须覆盖完整 destination 集合。

完整 \(N\times N\) 矩阵与未知行总量是两个不同问题。当前主任务可以保留已知 \(O_i\)：

\[
\widehat T_{ij}
=
O_i\,p_\theta(j\mid i).
\]

如果要进一步预测总出流量，需要额外学习 \(\widehat O_i\)。四周内不建议同时把这作为第二个主要创新目标，但应把主任务的已知边际条件写清楚。

---

**baseline 可以采用各自合理的模型和训练方式；需要对齐的是最终预测对象。**

你这一点是对的。neuroGravity 不应仅因为会更新权重就被排除，Atwal 原模型也不应只能拆成 building features 后才能比较。只要相关方法能够在当前数据上实现、训练并输出目标 OD 矩阵，都可以按其原生方式参加系统比较。

但同一排名仍然需要明确：预测的是哪一年、哪个区域、哪些 OD 单元，测试标签是否被用于训练，以及哪些真实边际已知。否则“都输出矩阵”仍可能是在回答不同任务。

建议保留三组有不同目的的对比。

**最终 OD 预测能力**这组，可以恢复 DG 原始比较逻辑中的 G、MFG、NG、DG，再加 radiation，以及实际能完整运行的较强 OD 模型。当前仓库的绘图读取这四类结果，但 `main.py` 写死 DG，所以其他方法仍需要补运行入口。Radiation 在当前源码里没有实现。

其中 G/MFG/NG/DG 很有用：它们分别帮助判断地理特征与非线性模型的作用。更复杂模型不必强行改成 DG。GlODGen 如果只能取得公开输出，可以保留为参考，但在确认年份、区域映射和输入信息之前，不宜直接据其分数推断 MapAgents 的机制优势。

**特征发现能力**这组，我建议优先补下面几个强对照，而不是扩展更多弱 role-play baseline。

| 对照 | 具体做法 | 回答的问题 |
|---|---|---|
| DG + 原有特征表 | 重建已知的基础 OSM 特征 | 原始参照 |
| DG + 大型固定特征库 | 将可计算的细粒度 OSM 聚合特征一起输入 | 是否只是更多特征带来收益 |
| DG + 数据驱动搜索 | 对相同基础算子产生的候选进行前向/局部搜索 | 是否需要语言与领域知识 |
| DG + RAG 一次性程序 | 检索后一次生成并训练，不读残差 | 闭环反馈是否必要 |
| DG + 单 agent RAG 工具循环 | 同一个 agent 完成检索、执行与修改 | 多 agent 分工是否必要 |
| MapAgents | 三角色联合闭环 | 完整方法 |

数据驱动搜索不应仅搜索一个特别贫乏的标签清单。最好允许它使用相同基础算子中的部分尺度和组合，否则系统优势可能只是候选空间更大。

随机特征子集仍可作为低成本参照，但不需要把它当主要竞争者。Lasso、重要性排序、遗传搜索、贝叶斯搜索也不必全部堆上：选择一两个有代表性且实现可信的方法就够了。

**适配能力**这组，采用前面的源模型、仅参数适配、程序重新拟合和联合适配。尤其要有“保持原特征、给予额外训练机会”的对照，否则联合适配的收益可能来自训练次数增加。

系统级 flow baseline 不要求同一优化器或 epoch 数。用于证明 agent 协同的单 agent 对照，则应获得相当的检索、工具和候选评估机会，并报告实际 token、训练时间与搜索次数。

---

**评价应围绕最终 OD 质量，再用少量诊断解释程序为何有用。**

主指标继续使用标准 CPC：

\[
\mathrm{CPC}(T,\widehat T)
=
\frac{2\sum_{ij}\min(T_{ij},\widehat T_{ij})}
{\sum_{ij}T_{ij}+\sum_{ij}\widehat T_{ij}}.
\]

当总量相等时：

\[
\mathrm{CPC}
=
1-
\frac{\lVert T-\widehat T\rVert_1}
{2\sum_{ij}T_{ij}}.
\]

因此，它可以解释为归一化矩阵重合度，不宜写成识别了多少真实个体的正确目的地。

建议主表采用：

- 每城市 CPC，以及城市等权的平均结果；
- NRMSE；
- \(\log(1+T)\) 的 Pearson；
- 明确含义的 JSD。

JSD 我更建议定义为每个 origin 的真实与预测目的地分布之间的差异，再按指定方式汇总。它直接关心“流量送到了哪里”。原稿对所有 cell 数值画 histogram 后算 JSD，只比较流量值分布，打乱目的地后也可能不变，因此更适合作为分布诊断。

NRMSE 的分母要针对实际评估单元集合计算，不能在只评估部分 origin 时仍不加说明地套整个 \(N^2\)。所有指标都应清楚说明是否包含零流和对角线。

另加两个交通含义明确的诊断就足够：

- 目的地总流入量误差；
- 出行距离分布偏差。

如果某个 baseline 原生预测总量，应保留其原始矩阵，不应悄悄用真实 \(O_i\) 归一化。需要比较给定相同出流量时的目的地分配能力，可以另报一个明确标注的生产约束版本。

人工审计、二十个程序双人标注、Cohen’s \(\kappa\) 全部删除。自动证据重点保留程序执行成功率、检索证据可追踪率、候选修改收益、搜索成本和程序规模。

同时要降低对几个旧指标的解释强度：

- 高跨城市 Jaccard 不一定是失败，城市可能共享有效特征。
- 低 Jaccard 不一定表示发现了城市特性，也可能是不稳定。
- 不在 scene card 里的特征不一定是 hallucination，它可能是后续反馈产生的新发现。
- 有检索来源不等于语义解释一定正确。
- 特征删除后的预测变化表示模型依赖，不能直接当成出行因果关系。

---

**最值得做的机制实验，是直接检验“城市条件”和“残差反馈”是否发挥作用。**

四周内我建议优先安排以下对照：

1. **去掉出行知识检索。** 保留城市 OSM，判断交通知识是否比标签语义本身更有用。
2. **去掉城市动态证据。** 只给通用知识，判断系统是否依赖目标城市实际观测。
3. **去掉残差反馈。** 保留第一次 RAG 程序，判断迭代是否带来有效发现。
4. **只做 feature selection。** 允许选择标签，但禁止空间组合，判断收益是否来自程序构造。
5. **只更新模型参数。** 判断是否真的需要改变表征。
6. **单 agent 完成整个循环。** 判断三角色协作是否必要。

另外，我很推荐两个小而直接的分析。

一个是**程序跨城市置换**。把城市 A 的程序放到城市 B，用明确一致的拟合方式比较本地程序与外来程序。它比仅观察标签 Jaccard 更能回答“城市条件是否重要”。如果不同城市程序表现相近，论文就应承认发现的是通用好特征，而不是强行解释为城市专属机制。

另一个是**特征家族观测缺失下的适配**。在固定地理任务中移除某类输入标签，比较系统能否通过其他对象和空间关系恢复部分预测能力。这个实验可以直接检验 RAG、原始 OSM 和代理构造的结合。它是预先定义的数据扰动分析，结果应据实报告，不预设一定有效。

这两项比更多 reject-code 柱状图更有科学解释力。

---

**四周的安排应围绕一条完整链路逐步扩展。** 按你给定的时间约束，我建议这样组织后续研究；本次没有执行这些实验。

| 时间 | 主要工作 | 应交付的结果 |
|---|---|---|
| 第 1 周 | 完成一种通勤数据源的重建、两个城市的完整矩阵、原始 OSM 缓存、DG 与基础模型入口 | 数据—特征—模型—预测矩阵—指标全链路 |
| 第 2 周 | 完成静态/城市 RAG、空间程序表示、三角色闭环、可变特征 DG 训练 | 能根据误差提出、执行并评价程序修改 |
| 第 3 周 | 扩展到目标城市集合，完成主要 flow/feature baselines 和少样本联合适配 | 主结果、适配曲线、核心消融 |
| 第 4 周 | 补随机种子、程序置换或缺失分析、成本统计，完成写作 | 定稿表格、方法图、程序案例和研究结论 |

实施上最值得提前做的是**缓存原始 OSM 与基础空间计算**。每次程序修改都重新请求地图或重复空间连接，成本会迅速失控。缓存应覆盖原始对象和可复用几何操作，同时允许程序新增组合，不能退化成只有预制十八列可选。

搜索预算可以从每轮两个候选、三轮反馈起步，再依据实际单次 DG 训练成本调整。最终报告同时给出模型训练和 agent 搜索成本。不要预先承诺“改程序比 refit 更快”：新路线本来就包含模型训练，优势需要从最终质量、样本效率或搜索效率中获得。

旧稿的预期结果排序也应删除。大型固定特征库可能很强，单 agent 也可能足够好，某些城市可能完全不需要适配。新 proposal 应写清楚这些结果分别会支持什么结论，而不是提前规定各 baseline 排名。

我建议最终只保留三项主要贡献：

1. **开放词汇、检索支撑的空间特征程序发现。** 从原始 OSM 和出行知识构造区域属性与 OD 关系，超越固定标签清单。
2. **残差驱动的多 agent 程序—模型联合适配。** 预测误差决定下一步知识检索和空间构造，DG 随程序变化训练与迁移。
3. **完整 OD 矩阵上的城市泛化与少样本证据。** 用多城市通勤数据、强特征发现对照和机制实验，检验程序何时有用、如何迁移，以及付出了多少成本。

这些是基于本地材料提出的创新方向，是否已经被近邻工作覆盖仍未做文献核查；也没有实验结果支持性能承诺。现在最应该落定的，是**残差如何变成下一轮检索问题、候选空间程序和模型更新动作**。这部分做扎实，MapAgents 才会从一个 OSM 工具编排方案，发展为有明确学习机制的研究。